{% extends 'layout/layout.html.twig' %}

{% block head %}
    {{ post.title }}
{% endblock %}

{% block content %}
    <article class="mb-4">
        <div class="container px-4 px-lg-5">
            <div class="row gx-4 gx-lg-5 justify-content-center">
                <div class="col-md-10 col-lg-8 col-xl-7">
                    <h2 class="section-heading">{{ post.title }}</h2>
                    <span class="caption text-muted">Le {{ post.last_update|date("d / m / Y") }}</span>
                    <div class="category"><a href="{{ generate_route('category', { id:post.categoryId }) }}">{{ post.category }}</a></div>
                    <p>{{ post.lead }}</p>
                    <p>
                        <img src="assets/img/test-bg.jpg" alt="">
                    </p>
                    <p><img class="img-fluid" src="/img/uploads/{{ post.image }}" alt="..." /></p>
                    <p>{{ post.content }}</p>
                </div>
            </div>
        </div>
    </article>
{% endblock %}