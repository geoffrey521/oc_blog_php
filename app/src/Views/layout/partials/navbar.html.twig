<nav class="navbar NavbarCustom navbar-expand-lg" id="mainNav">
    <div class="container NavbarCustom-container px-4 px-lg-5">
        <a class="navbar-brand navbar-logo" href="{{ generate_route('home') }}"><img src="/assets/img/logo2white.png" alt="logo"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            Menu
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav py-4 py-lg-0">
                <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="{{ generate_route('home') }}">Accueil</a></li>
                <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="{{ generate_route('showPosts') }}">Tous les articles</a></li>
                <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="{{ generate_route('contact') }}">Contact</a></li>
                <li class="nav-item navbarCustom-link"><a class="nav-link px-lg-3 py-3 py-lg-4" href="calvino-geoffrey-cv.pdf" target="_blank">Télécharger mon cv</a></li>
                {% for page in menuPages %}
                    <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="{{ generate_route('showCustomPage', { slug:page.slug }) }}">{{ page.name }}</a></li>
                {% endfor %}
            </ul>
            <div class="navbar-auth py-4 py-lg-0 ms-auto">
                {% if session.user %}
                    {% if session.user.isAdmin  %}
                        <a href="{{ generate_route('admin') }}" class="btn navbar-auth-link">{{ session.user.username }}</a>
                    {% else %}
                        <a href="{{ generate_route('manageAccount') }}" class="btn navbar-auth-link">{{ session.user.username }}</a>
                    {% endif %}
                    <a href="{{ generate_route('logout') }}" class="btn navbar-auth-link">Se déconnecter</a>
                {% else %}
                    <a href="{{ generate_route('login') }}" class="btn navbar-auth-link">Se connecter</a>
                    <a href="{{ generate_route('register') }}" class="btn navbar-auth-link">Créer un compte</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
